Place reusable icon files here.

This folder can host:

- the existing Notion-style SVG icon set
- custom PNG or SVG icons used by the workspace

If you want a minimal initial commit, keep only the icons you actually plan to apply through hosted URLs.
